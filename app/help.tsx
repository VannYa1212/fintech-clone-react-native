import { useState } from 'react';
import { View, Text } from 'react-native';
const Page = () => {
  const [state, setState] = useState(0);
  
  
  return (
    <View>
      <Text>Page dd</Text>
    </View>
  );
};
export default Page;
